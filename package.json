{
  "name": "auto-suggest",
  "version": "1.0.0",
  "description": "visiting my git set up:)\r changes flow for posting\r 1.add(plus button)",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1"
  }
}
